<%= form_with(model: [@idea, @initiator_proposal], local: true) do |form| %>
  <%= form.hidden_field :account_id, value: current_account.id %>
  <%= form.label 'опишіть свій задум' %>
  <%= form.text_area :plan %>
  <div class="actions">
    <%= form.submit "опублікувати" %>
  </div>
<% end %>
